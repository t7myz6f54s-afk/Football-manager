// Top-level build file. Chaquopy embeds CPython in the APK so the existing
// Python simulation engine runs on the device — the game itself is unchanged.
plugins {
    id 'com.android.application' version '8.5.2' apply false
    id 'com.chaquo.python' version '17.0.0' apply false
}
